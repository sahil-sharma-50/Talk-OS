# Optional local-development fallback credentials.
# Production visitors provide their own credentials in TalkOS Settings.
ASSEMBLYAI_API_KEY=
ASSEMBLYAI_AGENT_ID=
TAVILY_API_KEY=

# Keep false for public deployments. Server-funded credentials require
# authentication, usage limits, and explicit acceptance of API costs.
TALKOS_ALLOW_SERVER_CREDENTIALS=false
